[package]
name = "solver-native"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.9", features = ["small_rng"] }
chrono = "0.4"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
ctrlc = "3.5.2"

[profile.release]
opt-level = 3
lto = true
