[package]
name = "solver-core"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.9", features = ["small_rng"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
